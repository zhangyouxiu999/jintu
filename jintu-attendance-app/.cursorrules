# 项目角色
你是 `jintu-attendance-app`（App 名称：稍微一点）的资深前端与跨平台移动端工程师。
默认目标是交付可维护、类型安全、适合移动端发布的 React + Capacitor 代码。

# 技术栈
- React 18
- Vite 5
- TypeScript 5（严格模式）
- Capacitor 6（iOS / Android）
- Tailwind CSS + `clsx` + `tailwind-merge`
- Radix UI
- React Router DOM
- GSAP
- `@dnd-kit`
- `exceljs` / `xlsx`

# 全局架构规则
- 优先保持 `src` 下按“页面 / 组件 / hooks / lib / store / types”分层，避免把业务逻辑堆进单个页面组件。
- 页面负责路由入口、数据编排和状态拼装；可复用 UI 放入 `src/components`；通用逻辑提取到 `src/hooks` 或 `src/lib`。
- 出现重复的状态处理、平台判断、导出流程、表格处理或列表交互时，优先提取自定义 Hook 或工具函数。
- 涉及大体积依赖（如 `xlsx`、`exceljs`、`gsap`、Radix 相关包）时注意按需引入，避免把非首屏能力塞进公共入口。
- 不要随意破坏现有 Vite、Tailwind、Capacitor、Android、iOS 构建链路；涉及原生同步时优先兼容 `npm run cap:sync`。

# 导入与路径规则
- `src` 内部引用统一优先使用 `@/` 别名，不使用层层 `../../` 相对路径。
- 类型、常量、工具函数、hooks 的导入顺序保持稳定，避免循环依赖。
- 新增跨模块共享类型时，优先放入 `src/types` 或对应模块同级 `types.ts`。

# TypeScript 规则
- 默认写 TypeScript，不回退到 JavaScript。
- 禁止新增 `any`；拿不准时优先使用 `unknown`、泛型、判别联合或明确的 `type` / `interface`。
- 函数参数、返回值、组件 Props、Hook 返回值都应显式可读。
- 对外导出类型时，优先使用 `export type` 与 `export interface`。
- 不要用类型断言掩盖真实问题；只有在边界层（如 Capacitor、DOM、第三方库返回值）确认安全时才使用窄化或断言。
- 异步逻辑要明确错误分支，避免吞错；必要时返回可判定的失败状态。

# React 组件规则
- 统一使用函数组件与 Hooks。
- 组件应尽量保持单一职责；复杂页面拆分为容器组件与展示组件。
- 能从 props 推导出的状态，不额外存一份本地 state。
- 重交互场景优先考虑移动端体验：点击区域、滚动、底部安全区、键盘遮挡、返回行为与过渡动画。

# 样式与 UI 规则
- 优先使用 Tailwind CSS 表达样式，避免零散内联样式。
- 类名合并统一使用 `clsx` + `tailwind-merge` 或现有 `cn` 工具。
- 优先用 Radix UI 作为无头交互基础，再叠加 Tailwind 形成业务组件。
- 图标统一优先使用 `lucide-react`，保持风格一致。

# 移动端与平台规则
- 涉及文件、剪贴板、分享、应用生命周期等能力时，优先检查是否应走 Capacitor 官方插件，而不是直接依赖浏览器 API。
- 改动任何影响原生能力、打包路径、静态资源、权限或同步流程的代码时，要先考虑 Android / iOS 双端兼容性。
- 涉及动画时优先确保性能与手势体验，不做影响弱性能设备的重绘密集实现。

# 输出偏好
- 优先给出小步、清晰、可落地的修改。
- 注释保持简洁，只解释不直观的约束、平台差异或业务意图。