# 多余文件检查

基于「是否有被引用 / 是否重复」对项目内文件做检查，结论如下。

---

## 一、已删除的未使用组件

| 文件 | 说明 |
|------|------|
| **src/components/PageTransition.tsx** | 已删除；未被任何地方 import，路由动效已由 AnimatedRoutes 内联实现。 |
| **src/components/RouteLoadingFallback.tsx** | 已删除；未被引用，Suspense fallback 在 AnimatedRoutes 中内联实现。 |

---

## 二、已删除的未使用导出

| 位置 | 说明 |
|------|------|
| **src/lib/appConfig.ts** 中的 `isLoginConfigured()` | 已删除（项目内无引用）。 |
| **src/lib/gsap.ts** 中的 `runPageIn()` | 已删除（仅被已删的 PageTransition 使用）。 |

---

## 三、文档类（非多余，可选归档）

| 文件 | 说明 |
|------|------|
| **docs/根目录配置与文档重复检查.md** | 历史检查记录，建议已落实；保留可作追溯，删除也不影响当前使用。 |
| **docs/重复文件检查报告.md** | 针对整个 admin 仓库的重复文件报告，一次性结论；同上，保留或归档均可。 |

其余文档（README、商用化路线图、售卖账号说明、App 更新与数据保留、ANDROID_BUILD、IOS_BUILD、带签证打包、ANDROID-兼容性、从0到落地、docs/README 索引、项目与文档符合性检查）均被 README 或索引引用，**非多余**。

---

## 四、脚本与配置

- **scripts/serve-apk.sh**：被 `npm run apk:serve` 使用，**保留**。
- **scripts/install-android-sdk.sh、install-jdk-macos.sh**：未被 package.json 引用，但可作为本地环境安装说明的配套脚本，**保留**即可。
- 各配置文件（vite、tsconfig、capacitor、tailwind、postcss、eslint）均唯一且在用，**无多余**。

---

## 五、小结

- **已删除**：`PageTransition.tsx`、`RouteLoadingFallback.tsx`、`appConfig.isLoginConfigured`、`gsap.runPageIn`。
- **后续清理继续删除**：`src/hooks/useAttendanceStats.ts`、`src/components/WeekScheduleGrid.tsx`、`src/lib/gsap.ts`，以及 `output/playwright/` 下的本地调试截图产物。
- **可选**：将「根目录配置与文档重复检查」「重复文件检查报告」归档或删除。
- 其余文件均有引用或独立用途，不视为多余。
