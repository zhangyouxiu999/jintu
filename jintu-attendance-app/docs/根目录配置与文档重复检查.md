# 根目录项目配置与文档重复检查

检查范围：**jintu-attendance-app** 根目录及 **docs/** 下的配置与文档。  
检查时间：2025-03-03。  
**状态**：历史检查记录，供后续整理参考。

**已执行**：已删除 ANDROID-总结.md，以 ANDROID_BUILD.md 为 Android 主入口；README 中已有「打包与分发」并链接至 ANDROID_BUILD、docs/带签证打包-Android、IOS_BUILD、App更新与数据保留、docs/README（文档索引）。

---

## 一、文档重复情况

### 1. Android 相关文档（重复较多）

| 文件 | 位置 | 内容概要 | 与其它文档关系 |
|------|------|----------|----------------|
| **ANDROID_BUILD.md** | 根目录 | 完整 Android 打包说明：环境、JDK/SDK 安装、日常流程、快捷命令、release/签证、分发方式、常见报错 | 主文档，内容最全 |
| **ANDROID-总结.md** | 根目录 | （**已删除**）原为 ANDROID_BUILD 的精简总结，与 ANDROID_BUILD 大量重复 | 已合并入口，以 ANDROID_BUILD 为主 |
| **docs/带签证打包-Android.md** | docs/ | 专注 Release 签名：keystore 配置、keytool 生成、打包命令、常见报错 | 被 ANDROID_BUILD 引用；与 ANDROID_BUILD 的「方式二」有部分重复 |
| **docs/ANDROID-兼容性.md** | docs/ | 兼容性说明：返回键、剪贴板、导出、安全区、存储等 | **与打包文档不重复**，可保留 |
| **android/README-SDK.md** | android/ | 仅「安装 Android SDK」：Android Studio 与命令行两种方式 | 被 ANDROID_BUILD 引用，**不重复** |

**结论与建议：**

- **ANDROID-总结** 与 **ANDROID_BUILD** 功能重复：同一套「环境、命令、APK 位置、分发、报错、签证」写了两遍，维护成本高且易不一致。
  - **建议**：二选一保留为主入口。  
    - 方案 A：只保留 **ANDROID_BUILD.md**，删除 ANDROID-总结，或在 README 中加一句「Android 打包见 [ANDROID_BUILD.md](./ANDROID_BUILD.md)」。  
    - 方案 B：保留 **ANDROID-总结** 作为「一页速查」，将 ANDROID_BUILD 改为「详细版」，并删掉与总结重复的段落（如环境表、分发表、报错表），只留更细的步骤与说明。
- **ANDROID_BUILD** 与 **docs/带签证打包-Android.md**：BUILD 已正确引用 doc，仅「方式二」有一小段签证说明与 doc 重叠，可接受；若想再精简，可将 BUILD 的「方式二」缩成「命令 + 详见 docs/带签证打包-Android.md」。

---

### 2. 其它文档（无重复或仅引用关系）

| 文件 | 说明 |
|------|------|
| **README.md** | 项目简介、从 0 到落地入口（指向 从0到落地步骤）、本地运行与构建 | 与 从0到落地步骤 是入口 vs 详细教程，**不重复** |
| **从0到落地步骤.md** | 从零创建项目到上架的全流程（阶段一～八） | 与 ANDROID/IOS_BUILD 不重复：前者是「第一次建项目+加平台」，后者是「已有项目如何打安装包」 |
| **IOS_BUILD.md** | iOS 环境、CocoaPods、打包流程、快捷命令 | 与 Android 文档并列，**不重复** |

---

## 二、根目录配置（无重复）

根目录当前配置与用途：

| 文件 | 用途 |
|------|------|
| **.env.example** | 环境变量模板（登录等） |
| **.gitignore** | Git 忽略规则 |
| **eslint.config.js** | ESLint |
| **postcss.config.js** | PostCSS |
| **tailwind.config.js** | Tailwind |
| **tsconfig.json** / **tsconfig.node.json** | TypeScript（含 Node 构建） |
| **vite.config.ts** | Vite 构建与别名 |
| **capacitor.config.ts** | Capacitor 应用与 webDir |
| **package.json** | 依赖与脚本 |
| **index.html** | 入口 HTML |

- 同一功能**只有一份**配置，没有「两个文件干同一件事」的重复。
- 与仓库上级 **admin** 的配置（Next.js、Jest、ESLint 等）属于**不同项目**，不视为应合并的重复。

---

## 三、建议执行清单（已落实）

1. **Android 文档**  
   - [x] 以 **ANDROID_BUILD** 为主入口，**ANDROID-总结** 已删除。  

2. **README**  
   - [x] README 已有「打包与分发」：本地构建、Android（ANDROID_BUILD、带签证打包）、iOS、App 更新与数据保留、文档索引。  

3. **其它**  
   - docs/ANDROID-兼容性.md、android/README-SDK.md、从0到落地步骤、IOS_BUILD 保持现状；文档总览见 [docs/README.md](./README.md)。

---

## 四、总结

- **存在明显重复**：根目录 **ANDROID-总结.md** 与 **ANDROID_BUILD.md** 内容高度重叠，建议合并入口或删除其一。  
- **文档引用关系**：带签证打包、ANDROID-兼容性、README-SDK 与主文档之间是引用/互补，不是重复。  
- **根目录配置**：单项目单套配置，无重复；与 admin 为两套项目，不必合并。
