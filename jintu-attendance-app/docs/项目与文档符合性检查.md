# 项目与文档符合性检查

检查时间：按当前代码与文档快照。  
目的：确认项目实现与 README、商用化路线图、售卖账号说明、App 更新与数据保留、Android/iOS 打包等文档描述一致。

---

## 一、README 与项目概览

| 文档描述 | 实际实现 | 结果 |
|----------|----------|------|
| Vite + React + TypeScript + Tailwind | package.json：vite、react、react-dom、typescript、tailwindcss、postcss、autoprefixer | ✅ 符合 |
| HashRouter 与页面：登录、班级列表、点名、历史、课程表、成绩、模板、设置 | App.tsx 使用 HashRouter；AnimatedRoutes 注册 /login、/、/attendance/:classId、/history、/history/:classId、/schedule/:classId、/grades/:classId、/templates、/settings | ✅ 符合 |
| 本地数据层（localStorage） | src/store/storage.ts 使用 localStorage，键前缀 jintu_attendance_ | ✅ 符合 |
| src/types 与《最优技术方案》一致 | types/index.ts 含 ClassEntity、StudentEntity、AttendanceSnapshot、AnnouncementEntity、Student | ✅ 符合 |
| Capacitor 6：Share、Filesystem、安全区等已接入 | 依赖 @capacitor/core@^6、@capacitor/share、@capacitor/filesystem、@capacitor/app 等；shareOrDownload 用 Filesystem+Share；index.css 与页面使用 --safe-*、viewport-fit=cover | ✅ 符合 |

---

## 二、商用化路线图（阶段 1：安全与配置）

| 文档描述 | 实际实现 | 结果 |
|----------|----------|------|
| 生产环境禁用默认账号，未配置则登录页提示「未配置登录凭据」 | appConfig.ts：import.meta.env.PROD 时若未配置 VITE_LOGIN_USERNAME/PASSWORD 则 getLoginCredentials() 返回 null；Login.tsx 提交时若 null 则 setError('系统未配置登录凭据，请联系管理员') | ✅ 符合 |
| 品牌可配置（VITE_APP_NAME） | appConfig.getAppName() 读取 VITE_APP_NAME；Login、ClassList、main.tsx document.title 使用 getAppName() | ✅ 符合 |
| .env.example 与 README 说明生产必填 | .env.example 含 VITE_APP_NAME、VITE_LOGIN_USERNAME、VITE_LOGIN_PASSWORD 及生产必读说明；README 商用部署小节有对应说明 | ✅ 符合 |

---

## 三、售卖账号模式说明

| 文档描述 | 实际实现 | 结果 |
|----------|----------|------|
| 方式一：构建时写入 VITE_LOGIN_USERNAME / VITE_LOGIN_PASSWORD | 生产构建下由 appConfig.getLoginCredentials() 从环境变量读取，未配置则不可登录 | ✅ 符合 |
| 方式二：需增加登录时调后端校验 | 文档已写明「需增加」；当前登录仅前端校验凭据，未调用外部接口 | ✅ 符合（文档未声称已实现方式二） |

---

## 四、App 更新与数据保留

| 文档描述 | 实际实现 | 结果 |
|----------|----------|------|
| 数据在 localStorage，键前缀 jintu_attendance_ | storage.ts：PREFIX = 'jintu_attendance_'，STORAGE_KEYS 使用该前缀 | ✅ 符合 |
| 存储键：classes、students、attendance、announcements、appTitle、auth、schedule、grades | STORAGE_KEYS 包含上述键，无多余或缺失 | ✅ 符合 |
| 不要改应用身份（applicationId / Bundle ID） | capacitor.config.ts appId: 'io.jintu.attendance'；android/app/build.gradle applicationId "io.jintu.attendance" | ✅ 符合（发布时需保持与文档一致） |

---

## 五、ANDROID_BUILD 与带签证打包

| 文档描述 | 实际实现 | 结果 |
|----------|----------|------|
| Capacitor 6、android/ 工程 | package.json @capacitor/android ^6；存在 android/ 目录 | ✅ 符合 |
| npm run apk:debug、apk:release、android、ios、cap:sync | package.json 含对应 scripts | ✅ 符合 |
| 可安装包路径 app-debug.apk / app-release.apk | 与 Gradle 默认输出路径一致 | ✅ 符合 |
| applicationId io.jintu.attendance | android/app/build.gradle 中为 io.jintu.attendance | ✅ 符合 |
| 带签证打包：keystore.properties、.jks、build.gradle 读取 | docs/带签证打包-Android 所述文件存在；android/.gitignore 含 keystore.properties、*.jks；存在 keystore.properties.example | ✅ 符合 |

---

## 六、ANDROID-兼容性

| 文档描述 | 实际实现 | 结果 |
|----------|----------|------|
| BackHandler 使用 @capacitor/app 的 backButton | BackHandler.tsx 动态 import @capacitor/app，addListener('backButton')，根路由 exitApp 否则 history.back() | ✅ 符合 |
| 剪贴板：原生用 @capacitor/clipboard | src/lib/clipboard.ts 在 Capacitor.isNativePlatform() 时使用 @capacitor/clipboard | ✅ 符合 |
| 导出：原生用 Filesystem 写 Cache + Share | shareOrDownload.ts 在 isNativePlatform() 时 Filesystem.writeFile(Directory.Cache) + Share.share | ✅ 符合 |
| viewport-fit=cover、safe-area | index.html viewport 含 viewport-fit=cover；index.css 定义 --safe-* 使用 env(safe-area-inset-*, 0px)；部分页面使用 paddingTop/Bottom 或 pb-[env(...)] | ✅ 符合 |
| minSdkVersion 22、targetSdkVersion 34 | android/variables.gradle 中 minSdkVersion = 22、targetSdkVersion = 34 | ✅ 符合 |

---

## 七、从0到落地步骤（结构对照）

| 文档要求 | 实际实现 | 结果 |
|----------|----------|------|
| store：db.ts、classes、students、attendance、announcements | 均存在且对外接口与文档描述一致（getAll/getById/insert/update/remove、hydrateFromPersisted 等） | ✅ 符合 |
| hooks：useClassList、useClass、useAttendance、useAnnouncements | 均存在 | ✅ 符合 |
| 路由：HashRouter，/、/attendance/:classId、/history、/settings | 已实现且包含 /login、/schedule/:classId、/grades/:classId、/templates | ✅ 符合（为文档超集） |
| 数据层可选 SQLite / 先用内存或 localStorage | 当前采用 localStorage，文档已注明「当前实现为 localStorage」 | ✅ 符合 |

---

## 八、IOS_BUILD

| 文档描述 | 说明 | 结果 |
|----------|------|------|
| Capacitor 6、ios/ 工程、Xcode、CocoaPods | 依赖与目录存在；具体环境以本机为准 | ✅ 文档与工程一致 |
| appId io.jintu.attendance | capacitor.config.ts 中一致 | ✅ 符合 |

---

## 九、小结

- **符合项**：README 技术栈与页面、商用阶段 1（生产禁用默认账号、品牌可配置、.env 说明）、售卖账号方式一、App 更新与数据保留约定、ANDROID_BUILD/带签证打包、ANDROID-兼容性、从0到落地步骤中的 store/hooks/路由与数据层，均与文档一致。
- **无不符合项**：未发现与文档矛盾的实现。
- **建议**：后续若实现「售卖账号方式二」（登录调后端校验）或增加数据版本与迁移逻辑，需同步更新对应文档与本文检查表。
